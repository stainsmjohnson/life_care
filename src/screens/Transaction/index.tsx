import { View, Text } from 'react-native';
import React from 'react';
import { API_URL } from 'config/constants';

const Transaction = () => {
  return (
    <View>
      <Text>Transaction -{API_URL}</Text>
    </View>
  );
};

export default Transaction;
